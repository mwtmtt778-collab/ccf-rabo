# 04 重要决策

## 运行产物不写入 docs

- [DOCUMENTED] `docs/GIT_WORKFLOW_REPORT.md` 记录：运行脚本生成的 Markdown / JSON / PPM 应写入 `outputs/`，日志写入 `logs/`，避免污染被跟踪的 `docs/`。
- [VERIFIED] `.gitignore` 当前忽略 `logs/` 和 `outputs/`。

## 三螺母 Expert 默认 dry-run

- [DOCUMENTED] `docs/RABO_THREE_NUT_EXPERT_IMPLEMENTATION_REPORT.md` 记录：真实机器人 / 仿真运动必须显式 `--execute`。
- [DOCUMENTED] dry-run 成功不等价于物理任务成功。

## 工作空间 V2 先坐标门禁

- [DOCUMENTED] `.ai/archive/project_history/RABO_WORKSPACE_V2_CLOSURE_REPORT.md` 记录：此前 Box A/B/C 坐标、左右臂 base pose、Legacy 右臂交叉验证未通过前，不运行完整 6x2 pose_check。
- [VERIFIED] 当前 `tools/resolve_workspace_coordinates.py` diff 已纳入左右臂 base_link world pose，并将 Box A/B/C 几何中心标记为工作空间测试非必需项。
- [PENDING] 工作空间 V2 仍需先通过本地验证，再进入 Rabo 现场完整 6x2 pose_check。

## 不把 Secret 写进代码

- [DOCUMENTED] README 和 `agents/example_agent/config.py` 都要求 API Key / Token 走环境变量。
- [DOCUMENTED] Rabo LLM Key 使用 `RABO_LLM_KEY`。

## Git 里保留源码和固定文档，运行产物进入 ignored 目录

- [DOCUMENTED] `docs/GIT_WORKFLOW_REPORT.md` 记录过 `outputs/` 从 Git tracking 中移除，文件留在本地。
- [VERIFIED] `agent_system_backup.tar.gz` 当前未被 Git 跟踪，且 `.gitignore` 已忽略本地备份包。
