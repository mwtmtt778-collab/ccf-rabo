# 02 系统环境

## 本地开发机

- [VERIFIED] OS：Ubuntu 22.04.5 LTS。
- [VERIFIED] Python：3.10.12，路径 `/usr/bin/python3`。
- [VERIFIED] ROS2 命令存在，路径 `/opt/ros/humble/bin/ros2`。
- [VERIFIED] 环境变量显示 `ROS_DISTRO=humble`、`ROS_VERSION=2`、`ROS_PYTHON_VERSION=3`。
- [VERIFIED] CUDA toolkit：`nvcc` 显示 CUDA 12.1。
- [VERIFIED] `nvidia-smi` 当前无法连接 NVIDIA driver，因此 GPU / Driver 可用性未知。
- [VERIFIED] Conda 命令存在，当前 `CONDA_SHLVL=0`。
- [VERIFIED] 本机当前未安装 `openai`、`rabo-dev-kit`、`rabo_robocap`。

## Rabo 云端环境

- [DOCUMENTED] `docs/RABO_RUNTIME_INTERFACE_MAP_REPORT.md` 记录过一次云端环境：Python 3.12.3、ROS_DISTRO jazzy、RMW `rmw_cyclonedds_cpp`、runtime namespace `/gs_1eebee6f37512bbc1d125b25511e912c`。
- [DOCUMENTED] `docs/RABO_RUNTIME_PROBE_METHOD_REPORT.md` 记录本机无法直接发现云端 Rabo runtime 接口。
- [PENDING] 当前 Rabo 机器真实环境需要现场重新确认。

## Python 依赖

- [DOCUMENTED] `requirements.txt` 包含：
  - `openai>=1.0`
  - `rabo-dev-kit`
  - `requests`
  - `rabo_robocap`
- [DOCUMENTED] `rabo_robocap` 是机器人控制 SDK，`rabo_dev_kit` 提供平台工具能力。

## 环境记录原则

- [VERIFIED] Git 仓库应记录 `requirements.txt`、运行说明、安全边界和环境恢复步骤。
- [PENDING] 机器状态如 ROS source、CUDA driver、Rabo 注入环境变量、runtime namespace 需要运行时检查，不应假装固定。

