# LLM 客户端 (OpenAI 兼容协议, 支持 OpenAI / DeepSeek / Qwen / Kimi /
# 自部署 vLLM / Ollama 等)
openai>=1.0

# 平台输入通道 RemoteControl 来自 rabo_dev_kit (example_agent 用到)
rabo-dev-kit
requests
numpy>=1.21
Pillow>=9.0
# 控制机器人时再取消注释 (example_agent 没用到):
# 凡 import 的第三方包都要列在这里, 否则平台运行环境装不上、import 失败。
rabo_robocap        # 机器人控制 SDK (机械臂/底盘/夹爪)
